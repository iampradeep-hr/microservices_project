server:
  port: 8084


spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/iampradeep-hr/microservices_project
          clone-on-start: true
